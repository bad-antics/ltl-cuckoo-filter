// examples/demo.ltl — runnable demo for ltl_cuckoo_filter

import std::io
use ltl_cuckoo_filter as lib

fn main() {
    io::println("ltl_cuckoo_filter demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}