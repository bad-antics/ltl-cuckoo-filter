// ltl-cuckoo-filter/main.ltl
//
// Cuckoo filter supporting delete and counting.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-cuckoo-filter v0.1.0")
    io::println("Cuckoo filter supporting delete and counting.")
    0
}
